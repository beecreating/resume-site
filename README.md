1. Install dependencies
npm install

2. Start the development server
npm run dev


This will:

Start the TypeScript server via nodemon

Watch for changes inside the src folder

Serve your site locally at:
👉 http://localhost:3000

3. Start Tailwind (if developing styles)

If Tailwind is not starting automatically, run:

npm run tw:dev